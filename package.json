{
  "name": "tezba-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd claude-flow/intelligent-searcher/client && npm install && npm run build",
    "install": "cd claude-flow/intelligent-searcher/client && npm install"
  }
}